"resource/layout/toolwindow_toolwindowsheet.layout"
{
	controls
	{
		// the container
		tab	{ controlname="PageTab" }
		TabMenuButton	{ controlname="MenuButton" }
	}
	layout
	{
		region { name=crop height=max margin-right=86 }
		region { name=full height=max width=max }
		place { region=crop control=*tab height=24 }
		place { region=full control=TabMenuButton height=24 }
	}
}